{
  "name": "stock-market-recommender",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "test": "yarn workspace front-end test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
